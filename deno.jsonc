{
    "tasks": {
        "app": "deno run --allow-all app.ts",
        "desktop": "sleep 1 && deno --allow-all desktop.ts | deno --allow-all webserver.ts",
        "web": "deno --allow-all webserver.ts open",
        "compile": "deno compile --allow-all --output dist/ app.ts"
    }
}